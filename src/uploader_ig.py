"""
uploader_ig.py
역할: 완성된 쇼츠 영상을 Instagram Reels에 업로드 (Facebook Graph API)
"""
import logging

logger = logging.getLogger(__name__)

def upload_to_instagram(video_path: str, caption: str):
    """
    Instagram Reels에 영상을 업로드하는 로직 (구현 필요).
    Facebook Graph API를 사용해야 합니다.
    """
    logger.info("=== Instagram Reels 업로드 시작 (현재는 스켈레톤 코드) ===")
    logger.warning("Instagram 업로드 기능은 아직 구현되지 않았습니다.")
    logger.warning("Facebook Graph API 연동 및 개발이 필요합니다. (Page ID, Access Token 등)")
    # TODO: Facebook Graph API를 사용하여 영상 업로드 및 퍼블리싱 로직 구현
    return "instagram_placeholder_id"